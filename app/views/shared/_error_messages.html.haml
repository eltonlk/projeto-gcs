- if resource.errors.any?
  .alert.alert-danger{ role: "alert" }
    %h6= t "errors.template.body", model: resource.class.model_name.human

    %ul.mb-0
      - resource.errors.full_messages.each do |message|
        %li= message
