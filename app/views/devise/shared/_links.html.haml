- if controller_name != 'sessions'
  .w-100.text-center= link_to t('.sign_in'), new_session_path(resource_name)

- if devise_mapping.registerable? && controller_name != 'registrations'
  .w-100.text-center= link_to t('.sign_up'), new_registration_path(resource_name)
